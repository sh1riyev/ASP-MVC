@using AspNet_MVC.ViewModels; 
@model HomeVM

<h1>Salam</h1>

@*<ul>
    @foreach (var item in Model.Students)
        {
            <li>@item</li>
        }
</ul>*@

<ul>
@*@for (int i = 0; i < Model.Students.Capacity; i++)
    {
        <li>@Model.Students[i]</li>
    }
</ul>*@

@*///HomeWork*@

<ul>
    @foreach (var item in Model.Stus)
        {
            <li>@(item.FullName + " " +item.Email+ " " +item.Age+ " " +item.Phone)</li>
        }
</ul>

    <br></br>

<ul>
    @foreach (var item in Model.Stus.OrderByDescending(m => m.Age))
    {
        <li >@(item.FullName + " " + item.Email + " " + item.Age + " " + item.Phone)</li>
}
</ul>

<h1>@Model.Products.Count</h1>

<ul>
    @foreach (var item in Model.Products)
        {
            <li>@(item.Name+ " " +item.Color+ " " +item.Price)</li>
        }
</ul>